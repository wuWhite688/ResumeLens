<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.arthur.jdragresume.mapper.JobDescriptionMapper">

    <select id="searchJobs" resultType="com.arthur.jdragresume.dto.JobListItem">
        SELECT id, title, company_name, location, employment_type, created_at
        FROM job_description
        WHERE user_id = #{userId}
        <!-- keyword 已由 JobQueryService.escapeLikeKeyword 转义，ESCAPE 字符须与 LIKE_ESCAPE 一致 -->
        <if test="keyword != null and keyword != ''">
            AND (title LIKE CONCAT('%', #{keyword}, '%') ESCAPE '!'
            OR company_name LIKE CONCAT('%', #{keyword}, '%') ESCAPE '!')
        </if>
        <if test="location != null and location != ''">
            AND location = #{location}
        </if>
        <!-- id 作为第二排序键：created_at 相同的记录顺序固定，翻页时不会重复或漏掉 -->
        ORDER BY created_at DESC, id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

</mapper>
